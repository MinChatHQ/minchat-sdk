{
  "name": "@minchat/js",
  "version": "4.0.9",
  "author": "MinChat",
  "description": "Minchat SDK for JS",
  "main": "dist/minchat-sdk.umd.js",
  "module": "dist/minchat-sdk.es.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "npm run clean && tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "test": "vitest",
    "push-dev": "npm run build && npm publish --tag dev",
    "push": "npm run build && npm publish"
  },
  "devDependencies": {
    "@types/socket.io": "^3.0.1",
    "cuid": "^3.0.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^3.2.3"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "socket.io-client": "^4.8.1"
  }
}
